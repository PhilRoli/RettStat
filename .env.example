# PocketBase Admin Credentials
# These are used to create the initial superuser account
PB_ADMIN_EMAIL=admin@rettstat.local
PB_ADMIN_PASSWORD=changeme

# Production PocketBase Admin (optional, falls back to PB_ADMIN_EMAIL/PASSWORD)
# PB_ADMIN_EMAIL_DEV=admin-dev@rettstat.local
# PB_ADMIN_PASSWORD_DEV=changeme-dev

# Production Environment Variables (for docker-compose.prod.yml)
# DOMAIN=example.com
# ACME_EMAIL=admin@example.com
# GITHUB_USERNAME=your-github-username
# GITHUB_TOKEN=your-github-token
# TRAEFIK_AUTH=admin:hashed-password
